# ChessTrainer - Локальный шахматный тренажёр

Локальное веб-приложение для тренировки шахматных навыков. Работает полностью офлайн, без подключения к интернету.

## Возможности

- **Тренажёр задач**: Решайте тактические задачи разной сложности
- **Свободная практика**: Играйте против себя, отрабатывайте дебюты и эндшпили
- **Теория**: Изучайте основы шахмат, ходы фигур и тактические приёмы
- **Каталог задач**: Фильтруйте задачи по сложности и выбирайте нужные

## Как запустить

### 🚀 Самый простой способ (Windows)

**Дважды кликните на файл `start.bat`** - приложение запустится автоматически!

Файл `start.bat`:
- Проверит наличие Node.js
- Установит зависимости (если нужно)
- Запустит сервер
- Откроет браузер

### 💻 На текущем компьютере (через терминал)

1. Убедитесь, что установлен Node.js (версия 16 или выше)
2. Откройте терминал в папке проекта
3. Установите зависимости:
   ```bash
   npm install
   ```
4. Запустите сервер разработки:
   ```bash
   npm run dev
   ```
5. Откройте браузер по адресу, который покажет Vite (обычно `http://localhost:5173`)

### 📦 Создание .exe файла (не требует Node.js на других ПК!)

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Соберите .exe:
   ```bash
   npm run electron:build
   ```

3. В папке `release/` появится установщик `ChessTrainer Setup X.X.X.exe`

4. **На другом компьютере** просто запустите установщик - Node.js НЕ требуется!

Подробные инструкции в файле [BUILD.md](BUILD.md)

### 🌐 На другом компьютере (без .exe)

1. **Скопируйте папку проекта** на флешку или другой носитель
   - ⚠️ **Важно**: Не копируйте папку `node_modules` (она очень большая)
   - Скопируйте только исходные файлы: `src/`, `public/`, `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `start.bat` и т.д.

2. **На новом компьютере**:
   - Установите Node.js с официального сайта: https://nodejs.org (выберите LTS версию)
   - Дважды кликните на `start.bat` или выполните в терминале:
     ```bash
     npm install
     npm run dev
     ```

3. Откройте браузер по указанному адресу

### Сборка для статического хостинга

Если хотите собрать готовую версию для размещения на веб-сервере:

```bash
npm run build
```

Готовая версия будет в папке `dist/`. Её можно загрузить на любой веб-хостинг или открыть локально через простой HTTP-сервер.

## Структура проекта

```
ChessTrainer/
├── src/
│   ├── components/     # React компоненты
│   ├── routes/         # Страницы приложения
│   ├── hooks/          # Кастомные хуки
│   ├── data/           # Данные (задачи, теория)
│   ├── styles/         # CSS стили
│   └── types/          # TypeScript типы
├── public/             # Статические файлы
├── package.json        # Зависимости проекта
└── vite.config.ts      # Конфигурация Vite
```

## Технологии

- React 18
- TypeScript
- Vite
- chess.js (движок шахматной логики)
- react-chessboard (компонент доски)
- react-router-dom (навигация)

## Режимы работы

### Тренажёр задач
- Решайте тактические задачи
- Получайте подсказки
- Отслеживайте статистику решений

### Свободная практика
- Играйте белыми или чёрными
- Отрабатывайте любые позиции
- Автоматическое определение мата с предложением начать заново

### Теория
- Изучайте правила и ходы фигур
- Практикуйтесь на интерактивных досках
- Осваивайте тактические приёмы

## Лицензия

Проект создан для личного использования и обучения.

